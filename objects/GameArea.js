function GameArea() {
  
  // CONSTANTS
  this.size = 900;
  this.topLeft = [0, 0]; // This will be actually be reset later on, but is essentially a constant.

  // METHODS
  this.display = function() {
    
  }
}